/*Evan O'Keeffe
  10324289
  practical 12 question 2
*/
#include<stdio.h>

#define AVG(x,y) (x+y)/2
#define AREA(x,y) (x)*(y)
   
//my defines that fixed them, i added more parenthesis to stop errors with subtraction and division for some examples
#define avg(x,y) ((((x)+(y)))/2)
#define area(x,y) ((x)*(y))

void partb(float a,float b);//function that shows an example for part b

int main()
{
	printf("the defines were changed to have extra parenthesis to fix problems with division and subtraction\r\n");
	return 0;
}
