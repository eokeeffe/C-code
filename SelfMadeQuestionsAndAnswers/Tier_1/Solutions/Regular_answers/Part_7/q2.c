/*
	Tier 1 : Part 7	:	Question 2
	
*/
#include<stdio.h>
#include<stdlib.h>

int main()
{
	//	the a++ means that the number held in a will be increment after it has been used , i.e that a=a+1;
	//	++a means pre increment , i.e a will be incremented before any it is used 
	//	--a same as above only decrement before being used
	//	a-- means post decremented , i.e will be decremented after use
	//	a+=b is the same as a=a+b only shorter
	//	a-b is nearly the same as above only a=a-b
	return 0;
}